# Prisma configuration for deployment
# Note: These configs may be needed for specific deployment environments
# Uncomment if deploying to environments that require them

# node-linker=hoisted
# public-hoist-pattern[]=*prisma*
# target=linux-musl 